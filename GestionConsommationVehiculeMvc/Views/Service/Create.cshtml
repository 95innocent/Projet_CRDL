@model GestionConsommationVehiculeMvc.Models.service

@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Layouts/_Default.cshtml";
}
	<div class="col-sm-9 col-sm-offset-3 col-lg-10 col-lg-offset-2 main"style="background-color:white">

<div class="fa fa-home" style="text-align:center; font-size:50px"> Administrateurs</div>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>

        <legend></legend>

            <div class="form-group">
                <label>NomService:</label>
                <input type="text" placeholder="Entrer NomService" required="required" autocomplete="off" class="form-control" name="NomService" />
            </div>
            <div class="form-group">
                <input type="submit" value="Enregistrer" class="form-control btn btn-info"/>
            </div>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

</div>
